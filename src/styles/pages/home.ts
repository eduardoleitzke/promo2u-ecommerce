import { styled } from "..";

export const HomeContainer = styled('main', {
    padding: '5rem',
    width: '100%',
    display: 'grid',
    alignItems: 'center',
    justifyItems: 'center',
    rowGap: '10rem',
    gridTemplate: 'auto auto auto auto/ auto auto auto auto',
})


