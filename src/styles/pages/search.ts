import { styled } from "..";

export const SearchContainer = styled('main', {
    padding: '5rem',
    width: '100%',
    display: 'grid',
    alignItems: 'center',
    justifyItems: 'center',
    rowGap: '10rem',
    gridTemplate: 'auto auto auto auto/ auto auto auto auto',
})